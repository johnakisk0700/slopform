NODE_ENV=production

# Edge / TLS. Point DOMAIN to the VPS before starting Caddy.
DOMAIN=app.legacy.example.com
ACME_EMAIL=ops@legacy.example.com

# Application
API_HOST=0.0.0.0
API_PORT=4000
WEB_ORIGIN=https://app.legacy.example.com
NUXT_PUBLIC_API_BASE=/api/v1
NUXT_API_BASE_INTERNAL=http://api:4000/api/v1

# Generate strong, URL-safe values. DATABASE_URL and REDIS_URL use container DNS.
POSTGRES_DB=join_the_six
POSTGRES_USER=join_the_six
POSTGRES_PASSWORD=replace_with_a_long_url_safe_secret
DATABASE_URL=postgresql://join_the_six:replace_with_a_long_url_safe_secret@postgres:5432/join_the_six
DATABASE_POOL_MAX=20
REDIS_PASSWORD=replace_with_a_long_url_safe_secret
REDIS_URL=redis://:replace_with_a_long_url_safe_secret@redis:6379

# Observability: enable OTLP or Sentry tracing, not both.
LOG_LEVEL=info
OTEL_SERVICE_NAME=join-the-six
OTEL_EXPORTER_OTLP_ENDPOINT=
SENTRY_DSN=
SENTRY_TRACES_SAMPLE_RATE=0.1

# Dashboard stays off unless it is intentionally exposed and authenticated.
BULL_BOARD_ENABLED=false
BULL_BOARD_USERNAME=
BULL_BOARD_PASSWORD=

# Transitional WordPress boundary. Server-side only.
WORDPRESS_BASE_URL=https://wordpress.example.com
WORDPRESS_SERVICE_TOKEN=
WORDPRESS_WEBHOOK_SECRET=
