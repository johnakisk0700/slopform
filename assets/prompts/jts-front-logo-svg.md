# Front logo prompt (2026-07-24)

> **Spent prompt.** It produced `assets/jts-front-logo.svg` and
> `assets/jts-front-logo-mark.svg`, which are the shipped marks. Kept so the
> logo can be regenerated on the same brief — this is not pending work.

Rebuild this exact brand mark as a clean production SVG logo.

Brand meaning (keep this composition — do not invent or replace icons):

- Flat cream / off-white shapes on a solid dark forest-green square background.
- One solid circle in the exact center (the table / shared hub).
- Exactly five stylized people arranged around the center, plus one empty seat.
- Each person is: small head circle + wider rounded shoulders / torso.
- The empty seat is the short horizontal capsule / pill at 6 o'clock — an empty chair waiting for the viewer. It is NOT a sixth person and NOT a trefoil.
- People sit at the other five positions on a regular 60° radial grid (12, 2, 4, 8, and 10 o'clock). The empty chair holds the remaining slot at 6 o'clock so the ring still reads as six places: five occupied + one open. That open place is “you” — Join the Six.

Geometry requirements (obsessive calibration of the reference):

- Perfect bilateral symmetry.
- Equal angular spacing on a 60° grid.
- Identical radial clearance from the central circle to every satellite (people and empty chair).
- Identical gaps between neighboring satellites.
- The five person marks are congruent (same size, same head/shoulder proportions); only rotation differs. Heads point outward from the hub.
- The empty chair is centered on the bottom axis, same distance from the hub as the people, same visual weight as a seat, clean capsule geometry.
- Smooth, mathematically clean curves — no hand wobble, no grain, no noise, no texture, no anti-alias fringes.
- Flat fills only; no gradients, no shadows.
- Centered in a square 1:1 canvas with even margin to the edge.

Keep the same two-color palette as the reference. Output real vector SVG paths suitable for a product logo.
