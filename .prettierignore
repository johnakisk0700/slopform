.claude
.turbo
coverage
dist
node_modules
pnpm-lock.yaml
reference
# Burst HTML and other local rehearsal output — gitignored, and not formatted.
report
tmp
# Emitted by `pnpm openapi:emit`; its serializer owns the formatting.
apps/backend/openapi/openapi.json
